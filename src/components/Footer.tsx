export function Footer() {
  return (
    <footer className="w-full p-8 text-center text-muted border-t border-bg-2 mt-auto">
      <p>© {new Date().getFullYear()} Orion Club. Every domain. One sky.</p>
    </footer>
  );
}
